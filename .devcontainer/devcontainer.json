{
  "updateContentCommand": "sudo bash -c \"$(wget -O - https://apt.llvm.org/llvm.sh)\" && curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y && rustup target add wasm32-unknown-unknown",
}
